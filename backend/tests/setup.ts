// Setup for backend tests using bun:test
// Add any global mocks or test DB connections here
import { beforeAll, afterAll } from 'bun:test'

beforeAll(() => {
  // Global setup before all tests run
})

afterAll(() => {
  // Global teardown after all tests run
})
